<p style="color: green"><%= notice %></p>

<%= render @job %>

<div>
  <%= link_to "Edit this job", edit_job_path(@job) %> |
  <%= link_to "Back to jobs", jobs_path %>
  
  <%= button_to "Destroy this job", @job, { method: :delete, data: { turbo_confirm: "Are you sure?" } } %>
</div>
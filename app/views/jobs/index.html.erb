<p style="color: green"><%= notice %></p>

<h1>Jobs List</h1>

<div id="jobs">
  <% @jobs.each do |job| %>
    <%= render job %>
    <p>
      <%= link_to "Show this job", job %>
    </p>
  <% end %>
  
  <%# ログインしているときのみ、表示させる %>
  <% if logged_in? %>
  <p>
    <%= link_to "New job", new_job_path %>
  </P>
  <% end %>
</div>